# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"
group :test do
gem "rspec"
end

gem "rest-client"
# sinatra gem with the specific version 2.0.2
gem "sinatra", "2.0.2"
# hashie gem without specifying a version
gem "hashie"
# octokit gem specifying version 2.0 with a twiddle-wakka
gem "octokit", "~> 2.0" 
# lists the awesome_print gem specifying a remote git repository using the SSH URL (use github)
gem "awesome_print",:git => "git@github.com:awesome-print/awesome_print.git" 

group :development do
  gem "pry"
end
